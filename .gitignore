# macOS
.DS_Store
/target/

# Secrets — never commit these
.env
.env.*
*.env
*.env.*

# Local recovery receipts for reviewed documentation changes.
/.central/documentation-transactions/

# Vendored reference trees (never built or published from this repo).
vendor/

# Agent/tool state (AIKit task worktrees, session scratch).
/.aikit/
/ProjectCentral/now/tmp/
